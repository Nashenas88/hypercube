# lib.rs

Declares all modules and exposes `pub fn run() -> iced::Result`, the thin `iced::application` setup that wires `app::HypercubeApp::new/update/view` together. Contains no UI or 3D/4D logic itself.
